package de.TeutonStudio.GalactiCraft.worldtype

/**
 * The authoritative world-type marker. Actual ServerLevels are supplied at runtime by
 * DynamicDimensions; this type owns the planet/stack invariants rather than a generator.
 */
enum class PlanetWorldType {
    GALACTICRAFT_PLANETARY,
}
